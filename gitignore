# Secrets — never commit these
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Wrangler local state
.wrangler/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Dependencies (only relevant if you add a package.json)
node_modules/
